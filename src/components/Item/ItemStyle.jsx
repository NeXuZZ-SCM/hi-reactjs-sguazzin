import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
        maxWidth: 345,
        width: 350,
        display: "inline-block",
      },
});

export { useStyles };